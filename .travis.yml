sudo: required

language: cpp

compiler:
  - gcc
  - clang

before_script:
  - PWD=`pwd`
  - git submodule update
  - cd $PWD/chapter5/googletest/googletest
  - g++ -std=c++11 -I./include/ -I. -c ./src/gtest-all.cc

script:
  - cd $PWD/chapter5/add01
  - make test
